import React from "react";
import Cursor from "../components/Cursor";

const page = () => {
  return (
    <div>
      <main className="flex min-h-screen flex-col items-center justify-between p-12">
        <div>I can&apos;t think of anything now. Will do it later.</div>
        <Cursor />
      </main>
    </div>
  );
};

export default page;
